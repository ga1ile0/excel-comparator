# Base URI of the application under test
test.base-uri=http://localhost:8080

# Database connection for running setup.sql scripts
test.db.url=jdbc:postgresql://localhost:5432/testdb
test.db.username=testuser
test.db.password=testpass

# Fetcher mode: 'direct' (single request) or 'async' (request + poll + download)
export.fetcher=direct
